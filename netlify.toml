# Send EVERY request, on every path, to the logger function.
[build]
  functions = "netlify/functions"
  publish   = "public"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from   = "/*"
  to     = "/.netlify/functions/log"
  status = 200
  force  = true

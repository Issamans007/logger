{
  "name": "octopus-listener",
  "version": "1.0.0",
  "private": true,
  "description": "Traffic listener for issamansour.com firewall testing — logs every hit to Supabase",
  "scripts": {
    "setup-db": "node scripts/setup-db.js",
    "test-log": "node scripts/test-log.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "pg": "^8.13.0"
  }
}
